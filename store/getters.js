const getters = {
	// token: state => state.user.token,
	// account: state => state.user.account,
	// user: state => state.user.user,
	// loading: state => state.user.loading,
	activebar: state => state.config.activebar
}
export default getters
